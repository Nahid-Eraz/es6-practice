const firstName = "Justin";
const lastName = "Timberlake";
const fullName  = firstName + ' ' + lastName;
const fullName2 = `${firstName} ${20+50+30} is a good boy.`;

console.log(fullName2);

const multiLine = `NO pain
No Gain
No life`;

console.log(multiLine)